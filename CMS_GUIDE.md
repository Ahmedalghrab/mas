# نظام إدارة المحتوى (CMS) الكامل ✨

تم إضافة نظام إدارة محتوى كامل يسمح بالتحكم في جميع أقسام الموقع من لوحة التحكم!

## 🎯 الميزات المضافة

### 1. إدارة الصفحات الديناميكية (Dynamic Pages)
**الرابط:** `/admin/pages`

**الإمكانيات:**
- ✅ إضافة صفحات جديدة بمحتوى مخصص
- ✅ تحرير وحذف الصفحات الموجودة
- ✅ دعم HTML في المحتوى
- ✅ دعم اللغتين العربية والإنجليزية
- ✅ روابط مخصصة (Slugs) مثل: `/page/about-us`
- ✅ صور مميزة للصفحات
- ✅ تحسين محركات البحث (SEO) - Meta Description & Keywords
- ✅ أيقونات مخصصة لكل صفحة
- ✅ التحكم في الظهور في القائمة الرئيسية
- ✅ التحكم في النشر (منشور/مسودة)
- ✅ ترتيب الصفحات حسب الأولوية

**مثال على الاستخدام:**
```
العنوان: سياسة الخصوصية
الرابط: privacy-policy
الأيقونة: bi-shield-lock
الظهور في القائمة: نعم
```
يصبح الرابط: `https://yoursite.com/page/privacy-policy`

---

### 2. إدارة محتوى الصفحة الرئيسية (Home Content)
**الرابط:** `/admin/home-content`

**الأقسام القابلة للتعديل:**
- 🎨 **قسم Hero (البطل):**
  - العنوان الرئيسي والفرعي
  - نص ورابط الزر
  - صورة الخلفية

- ⭐ **قسم المميزات (4 مميزات):**
  - عنوان كل ميزة
  - وصف كل ميزة
  - أيقونة مخصصة لكل ميزة

- 💼 **قسم الخدمات:**
  - عنوان القسم
  - وصف مختصر

- ℹ️ **قسم عن المنصة:**
  - نبذة عن المنصة
  - صورة توضيحية

**جميع الحقول بلغتين:** عربي وإنجليزي

---

### 3. إدارة محتوى صفحة "من نحن" (About Content)
**الرابط:** `/admin/about-content` (قريباً)

**الأقسام القابلة للتعديل:**
- العنوان الرئيسي والفرعي
- المحتوى الأساسي
- رؤيتنا (Vision)
- رسالتنا (Mission)
- قيمنا (3 قيم)
- قسم الفريق
- صورة رئيسية

---

### 4. إدارة محتوى صفحة "اتصل بنا" (Contact Content)
**الرابط:** `/admin/contact-content` (قريباً)

**الأقسام القابلة للتعديل:**
- عنوان الصفحة ووصفها
- معلومات الاتصال:
  - العنوان
  - رقم الهاتف
  - البريد الإلكتروني
  - رقم الواتساب
- ساعات العمل
- رابط الخريطة (Google Maps Embed)
- إعدادات النموذج
- رسالة النجاح بعد الإرسال

---

### 5. إدارة الأقسام (Categories)
**الرابط:** `/admin/categories` (موجود مسبقاً)

**الإمكانيات:**
- إضافة وتعديل وحذف التصنيفات
- أيقونات مخصصة لكل قسم
- دعم لغتين

---

### 6. إدارة الخدمات (Products/Services)
**الرابط:** `/admin/products` (موجود مسبقاً)

**الإمكانيات:**
- إضافة خدمات جديدة
- تحديد السعر والخصم
- صور للخدمات
- مدة التسليم
- ربط بالأقسام

---

## 📊 الجداول المضافة في قاعدة البيانات

```sql
1. Pages                -- الصفحات الديناميكية
2. HomeContents         -- محتوى الصفحة الرئيسية
3. AboutContents        -- محتوى صفحة من نحن
4. ContactContents      -- محتوى صفحة اتصل بنا
```

---

## 🔌 API Endpoints المضافة

### Pages API
```
GET    /api/Pages                    -- قائمة جميع الصفحات
GET    /api/Pages/{id}               -- صفحة محددة بالـ ID
GET    /api/Pages/slug/{slug}        -- صفحة محددة بالرابط
POST   /api/Pages                    -- إضافة صفحة جديدة
PUT    /api/Pages/{id}               -- تعديل صفحة
DELETE /api/Pages/{id}               -- حذف صفحة
```

### Home Content API
```
GET    /api/HomeContent              -- الحصول على المحتوى
PUT    /api/HomeContent              -- تحديث المحتوى
```

### About Content API
```
GET    /api/AboutContent             -- الحصول على المحتوى
PUT    /api/AboutContent             -- تحديث المحتوى
```

### Contact Content API
```
GET    /api/ContactContent           -- الحصول على المحتوى
PUT    /api/ContactContent           -- تحديث المحتوى
```

---

## 🚀 كيفية الاستخدام

### 1. الدخول إلى لوحة التحكم
```
الرابط: https://localhost:7008/admin
البريد: admin@mas.com
كلمة المرور: Admin@123
```

### 2. إضافة صفحة جديدة
1. اذهب إلى `/admin/pages`
2. اضغط على "إضافة صفحة جديدة"
3. املأ البيانات:
   - العنوان بالعربية (مطلوب)
   - العنوان بالإنجليزية (اختياري)
   - الرابط المخصص (Slug) - مثل: `privacy-policy`
   - المحتوى بالعربية (يدعم HTML)
   - الأيقونة - مثل: `bi-info-circle`
   - حدد إذا كانت منشورة أو مسودة
   - حدد إذا كنت تريدها تظهر في القائمة الرئيسية
4. احفظ

### 3. تعديل محتوى الصفحة الرئيسية
1. اذهب إلى `/admin/home-content`
2. عدّل أي من الأقسام
3. اضغط "حفظ التغييرات"
4. التغييرات ستظهر مباشرة في الموقع!

### 4. عرض الصفحات الديناميكية
بعد إضافة صفحة برابط `about-us`، يمكن الوصول إليها عبر:
```
https://localhost:7008/page/about-us
```

---

## 📱 الصفحات المضافة

### صفحات الإدارة (Admin Pages)
```
/admin/pages              -- إدارة الصفحات الديناميكية ✅
/admin/home-content       -- إدارة محتوى الصفحة الرئيسية ✅
/admin/about-content      -- إدارة محتوى صفحة من نحن (قريباً)
/admin/contact-content    -- إدارة محتوى صفحة اتصل بنا (قريباً)
/admin/categories         -- إدارة الأقسام (موجود مسبقاً)
/admin/products           -- إدارة الخدمات (موجود مسبقاً)
```

### صفحات العرض (Public Pages)
```
/page/{slug}              -- عرض الصفحات الديناميكية ✅
/                         -- الصفحة الرئيسية (تقرأ من HomeContent)
/about                    -- من نحن (ستقرأ من AboutContent)
/contact                  -- اتصل بنا (ستقرأ من ContactContent)
/services                 -- الخدمات (موجود مسبقاً)
```

---

## 🎨 أيقونات Bootstrap Icons المتاحة

يمكن استخدام أي أيقونة من Bootstrap Icons:
```
bi-house-door          -- منزل
bi-info-circle         -- معلومات
bi-envelope            -- بريد
bi-phone               -- هاتف
bi-shield-lock         -- خصوصية
bi-file-text           -- نص
bi-question-circle     -- سؤال
bi-star                -- نجمة
bi-check-circle        -- صح
bi-people              -- أشخاص
bi-briefcase           -- حقيبة
```

الكتالوج الكامل: https://icons.getbootstrap.com/

---

## 🔄 تدفق البيانات

### إضافة صفحة جديدة:
```
المستخدم (Admin)
    ↓
صفحة إدارة الصفحات (/admin/pages)
    ↓
إرسال POST request إلى /api/Pages
    ↓
حفظ في قاعدة البيانات (Pages table)
    ↓
الصفحة متاحة على /page/{slug}
```

### عرض صفحة ديناميكية:
```
الزائر يدخل /page/privacy-policy
    ↓
صفحة DynamicPage.razor تُحمّل
    ↓
إرسال GET request إلى /api/Pages/slug/privacy-policy
    ↓
عرض المحتوى مع التنسيق
```

---

## ⚙️ الإعدادات الإضافية

### 1. تفعيل الصفحات في القائمة الرئيسية
عند إنشاء صفحة، فعّل خيار "ظهور في القائمة" لتظهر في navigation menu

### 2. SEO (تحسين محركات البحث)
املأ الحقول التالية لكل صفحة:
- Meta Description (الوصف)
- Meta Keywords (الكلمات المفتاحية)
- العنوان بالإنجليزية (للظهور في نتائج البحث الإنجليزية)

### 3. الترتيب
استخدم حقل "DisplayOrder" لترتيب الصفحات في القوائم:
- 0 = أول صفحة
- 1 = الثانية
- وهكذا...

---

## 🚧 ما تبقى للتنفيذ

### صفحات الإدارة المتبقية:
- [ ] `/admin/about-content` - إدارة صفحة من نحن
- [ ] `/admin/contact-content` - إدارة صفحة اتصل بنا
- [ ] `/admin/testimonials` - إدارة آراء العملاء
- [ ] `/admin/faqs` - إدارة الأسئلة الشائعة
- [ ] `/admin/settings` - الإعدادات العامة

### ميزات إضافية:
- [ ] رفع الصور للصفحات
- [ ] معاينة الصفحة قبل النشر
- [ ] نسخ احتياطي للمحتوى
- [ ] سجل التعديلات
- [ ] محرر نصوص WYSIWYG (ما تراه هو ما تحصل عليه)

---

## 📝 ملاحظات مهمة

1. **الأمان:** جميع صفحات الإدارة محمية بسياسة `AdminOnly`
2. **الترميز:** جميع النصوص العربية تستخدم UTF-8
3. **المحتوى الافتراضي:** يتم إنشاء محتوى افتراضي تلقائياً للصفحة الرئيسية وغيرها
4. **الـ Slugs:** يجب أن تكون فريدة (لا يمكن تكرار نفس الرابط)
5. **HTML:** يمكن استخدام HTML في المحتوى، لكن احذر من XSS attacks

---

## 🎯 أمثلة عملية

### مثال 1: إضافة صفحة سياسة الخصوصية
```
العنوان: سياسة الخصوصية
Title: Privacy Policy
Slug: privacy-policy
الأيقونة: bi-shield-lock
المحتوى: <h3>نحن نحترم خصوصيتك...</h3><p>...</p>
منشور: نعم
في القائمة: نعم
```

### مثال 2: إضافة صفحة الأسئلة الشائعة
```
العنوان: الأسئلة الشائعة
Title: FAQ
Slug: faq
الأيقونة: bi-question-circle
المحتوى: <div class="accordion">...</div>
منشور: نعم
في القائمة: نعم
```

### مثال 3: تعديل Hero Section في الصفحة الرئيسية
```
اذهب إلى: /admin/home-content
عدّل:
  - العنوان: "أفضل منصة للخدمات الطلابية في المملكة"
  - الوصف: "نوفر لك خدمات أكاديمية وتقنية بجودة عالية"
  - نص الزر: "ابدأ الآن"
  - رابط الزر: /services
احفظ التغييرات
```

---

## 🎉 الخلاصة

الآن لديك نظام CMS كامل يسمح لك بـ:
✅ التحكم في كل محتوى الموقع
✅ إضافة صفحات غير محدودة
✅ تعديل النصوص بدون كود
✅ دعم لغتين كامل
✅ SEO محسّن
✅ واجهة سهلة الاستخدام

**جرّب الآن:** https://localhost:7008/admin 🚀
