# ? ?? ????? ??? JavaScript!

## ?? **???????:**

```
TypeError: el.className.includes is not a function
```

**?????:**
- ??? ??????? ?? DOM ??? `className` ?? ??? `SVGAnimatedString` ???? `string`
- ????? `.includes()` ???? ??? ?? `string`

---

## ? **???? ??????:**

### ???:
```javascript
if (!el.className || (!el.className.includes('bi') && !el.className.includes('bootstrap-icons'))) {
    el.style.fontFamily = "'Cairo', 'Tajawal', 'Segoe UI', 'Arial', sans-serif";
}
```

### ???:
```javascript
// Check if element has className and it's a string
if (el.className && typeof el.className === 'string') {
    // Skip if element has Bootstrap Icons classes
    if (!el.className.includes('bi') && !el.className.includes('bootstrap-icons')) {
        el.style.fontFamily = "'Cairo', 'Tajawal', 'Segoe UI', 'Arial', sans-serif";
    }
} else if (!el.className) {
 // Apply font to elements without className
    el.style.fontFamily = "'Cairo', 'Tajawal', 'Segoe UI', 'Arial', sans-serif";
}
```

---

## ?? **?? ?? ??????:**

### 1. **?????? ?? ??? className**
```javascript
typeof el.className === 'string'
```
????? ?? `className` ?? ?? ??? ??????? `.includes()`

### 2. **?????? ??????? ???? className**
```javascript
else if (!el.className) {
    el.style.fontFamily = "...";
}
```
???? ???? ??? ??????? ???? ??? ??? `className`

### 3. **???? ??????? ??????**
```javascript
if (!el.className.includes('bi') && !el.className.includes('bootstrap-icons'))
```
????? ??????? Bootstrap

---

## ?? **???????:**

### ??????? 1: Hot Reload (??? ??? ??????? ????)
??????? ???? ?? ??? Debug ????? Hot Reload!

**??? ???? ????? ????? ????? ????????? ????????** ?

### ??????? 2: ????? ???????
```bash
# ???? ???????
Ctrl + C

# ???? ??? ????
cd C:\halah\mas\mas
dotnet run
```

---

## ? **???????:**

- ? **?? ???? ??? JavaScript**
- ? **?????? ??????? ???? ???? ????**
- ? **??????? Bootstrap ???? ???? ?????**
- ? **?????? ??????? ????? ????? ???**

---

## ?? **??????:**

### 1. ???? ???????:
```
http://localhost:5212
```

### 2. ???? Console (F12):
??? ?? ???:
```
? Arabic fonts loaded successfully
? Arabic fonts applied to all elements
```

### 3. ?? ??? ?? ???:
```
? TypeError: el.className.includes is not a function
```

---

## ?? **???? ???????:**

| ??????? | ????? | ???? |
|---------|-------|------|
| TypeError | `className` ??? string | ?????? ?? ????? ????? |
| ??? ?? Console | ??????? `.includes()` ??? ??? string | `typeof === 'string'` |
| ????? SVG | ??? `SVGAnimatedString` | ?????? ???? |

---

## ?? **?? ??????? ?????!**

????:
- ? **?? ????? JavaScript**
- ? **?????? ???? ???? ????**
- ? **?????? ??????? ?????**
- ? **?????? ???? ??????**

---

## ?? **??????? ??????:**

### ????? ???? ????????

??? ????? DOM (???? SVG) ??? ????? `className` ?? ??? `SVGAnimatedString` ???? `string`:

```javascript
// ???? HTML ????
<div class="test">  // className = "test" (string)

// ???? SVG
<svg class="icon">  // className = SVGAnimatedString object
```

???? ????? ?????? ?? ????? ??? ??????? ??? String ??? `.includes()`

---

## ?? **???? ?????:**

1. ? ????? ????? ?????? (F5)
2. ? ??? ????? (Ctrl + Shift + R)
3. ? ?????? ???? ???????
4. ? ?????? ?? ??? ???? ??? ?? ??????

---

**??????? ???? ?????!** ??

---

**???? ?? ?? ?????? ????? JavaScript** ?
